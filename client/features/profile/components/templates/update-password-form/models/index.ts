import type { UseProfileUpdatePasswordFormType } from '~/features/profile/services';

export interface ProfileUpdatePasswordFormProps {
  loading?: boolean;
  form: UseProfileUpdatePasswordFormType;
}
